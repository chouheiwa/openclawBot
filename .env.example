# OpenClaw 环境变量配置示例
# 复制此文件为 .env 并填写你的实际值

# ===== AI 模型 API 密钥（至少配置一个）=====
# Anthropic Claude API (推荐)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API
# OPENAI_API_KEY=your_openai_api_key_here

# ===== Google Drive 备份配置（可选，推荐配置）=====
# 使用 Google Drive 自动备份 OpenClaw 数据，完全免费！

# Google Service Account JSON（从 Google Cloud Console 获取）
# RCLONE_CONFIG_GDRIVE_JSON='{"type":"service_account","project_id":"xxx","private_key_id":"xxx","private_key":"-----BEGIN PRIVATE KEY-----\nxxx\n-----END PRIVATE KEY-----\n","client_email":"xxx@xxx.iam.gserviceaccount.com","client_id":"xxx","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs"}'

# 备份位置（Google Drive 中的文件夹路径）
# BACKUP_REMOTE=gdrive:/openclaw_backup

# 备份间隔（分钟，默认 60 分钟）
# BACKUP_INTERVAL_MINUTES=60

# ===== PostgreSQL 数据库配置（可选，另一种持久化方案）=====
# 注意：配置了 Google Drive 备份后，通常不需要额外配置数据库
# 除非你需要多端同步或团队协作

# PostgreSQL 连接字符串（支持 Supabase、Neon、Railway 等）
# OPENPROSE_POSTGRES_URL=postgresql://user:password@host:port/database

# 示例：Supabase (免费 500MB)
# OPENPROSE_POSTGRES_URL=postgresql://postgres.xxxx:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# ===== 消息平台配置(按需配置) =====

# Telegram Bot
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# WhatsApp
# WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token_here
# WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here

# Discord
# DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Slack
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here
# SLACK_APP_TOKEN=xapp-your-slack-app-token-here

# ===== Web 终端配置(可选,推荐配置) =====
# Web 终端用于在浏览器中访问系统 Shell,方便运行时配置 OpenClaw

# Web 终端登录用户名(默认: admin)
TTYD_USERNAME=admin

# Web 终端登录密码(强烈建议修改为强密码!)
TTYD_PASSWORD=changeme_to_secure_password

# ===== OpenClaw Gateway 配置(可选) =====
# OpenClaw Gateway 认证 Token(建议设置强随机字符串)
# 如果不设置,将使用默认的不安全 token: changeme-insecure-token
OPENCLAW_GATEWAY_TOKEN=your-secure-random-token-here

# ===== 其他配置 =====

# 服务端口（HuggingFace 固定为 7860，本地开发可修改）
# PORT=7860

# 日志级别
# LOG_LEVEL=info

# OpenClaw 数据目录（默认 ~/.openclaw）
# OPENCLAW_STATE_DIR=/home/user/.openclaw

# ===== HuggingFace Spaces 部署说明 =====
# 在 HuggingFace Space Settings > Variables and secrets 中配置上述环境变量
# 不要将真实的 API 密钥提交到 Git 仓库中
#
# 推荐配置（免费方案）：
# 1. 必需：ANTHROPIC_API_KEY 或 OPENAI_API_KEY
# 2. 推荐：RCLONE_CONFIG_GDRIVE_JSON (使用 Google Drive 免费 15GB 实现数据备份)
# 3. 可选：各种消息平台的 Token
#
# Google Drive 配置步骤：
# 1. 访问 Google Cloud Console (console.cloud.google.com)
# 2. 创建项目 > 启用 Google Drive API
# 3. 创建 Service Account > 下载 JSON 密钥
# 4. 在 Google Drive 中创建文件夹，共享给 Service Account 邮箱
# 5. 将 JSON 内容作为单行字符串配置到 RCLONE_CONFIG_GDRIVE_JSON
